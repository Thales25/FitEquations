<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TaxaDeMetabolismoBasal" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="android:statusBarColor">@color/azul_claro</item>
        <item name="android:navigationBarColor">@color/verde_azulado</item>

        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.TaxaDeMetabolismoBasal" parent="Base.Theme.TaxaDeMetabolismoBasal" />
    <style
        name="RadioButonn" parent="Theme.AppCompat.Light"/>
    <!--cor do radio button-->
    <item name="colorAccent" type="color">#29611F</item>
   <item name="colorButton" type="color">@color/white</item>

</resources>